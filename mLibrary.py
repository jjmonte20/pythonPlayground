import math
# module can be used to access the math library

print(math.floor(2.1))
# will always go to the bottom number

x = 2.9
print(round(x))
# number will round to the nearest integer
print(abs(-2.9))
# will always have a positive number