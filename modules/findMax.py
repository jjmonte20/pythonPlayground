from utils import find_max

numbers = [10, 2, 34, 5, 67, 8]
maximum = find_max(numbers)
# do not change the meaning of things in a function by calling your variable as the same name from the function
print(maximum)

# when a term is defined in a previous function, it can be used to call a function
