birth_year = input('Birth year: ')
age = 2019 - int(birth_year)
# input puts values as strings first, if we want to combine with ints, everything needs to be set as ints
print(age)

# Kilograms ex
weight_lbs = input("weightt (lbs): ")
weight_kg = float(weight_lbs) * 0.45
print(weight_kg)